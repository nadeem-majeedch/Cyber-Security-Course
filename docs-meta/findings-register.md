# Findings Register — Independent QA Audit
**Auditor: independent QA review · Date: 2026-09-19 · Evidence key: [V] verified by inspection/test · [N] not run — environment limitation**

| ID | Sev | Area | Finding | Evidence | Remediation | Status |
|---|---|---|---|---|---|---|
| C1 | **CRITICAL** | Assessments / Governance | Complete live assessment instruments (midterm + final papers with marking schemes, checkpoint keys, lab keys, case solutions, slide speaker notes) sit in a **public repository**. Any student can read every exam before sitting it. | [V] `git ls-remote` earlier showed public repo; files present under `assessments/instructor-only/` incl. `midterm-paper.md`, `final-paper.md` | Move the entire instructor-only tier to a private companion repo or campus LMS **before term start**; add a sync script; keep the public repo student-only. Blocker for any real delivery. | OPEN |
| H1 | **HIGH** | Assessments | **Checkpoint answer keys do not cover paper items 7–8.** All six checkpoint papers (quiz-02/04/06/10/12/14) have 8 numbered items; `checkpoint-keys.md` keys only 6 per checkpoint. Items 7–8 (the scenario items, 2 marks each by paper wording) are unkeyed — papers cannot be marked consistently. | [V] scripted paper-vs-key comparison: quiz-02 key gap `[7]`; quiz-04/06/10/12/14 gap `[7,8]` | Add keys for items 7–8 of each checkpoint (the model answers exist inside the corresponding teaching guides' answer-key sections — consolidate them into `checkpoint-keys.md` and re-run an extended validator check). | OPEN |
| H2 | **HIGH** | Website / Pages | **Website is not buildable.** `website/index-template.md` is a permission-locked **directory** (anomalous artifact, unreadable), `website/lectures/` holds 7 empty module dirs (module-08 missing), no `index.md`/`index.html`, no `.github/workflows/pages.yml`. Nothing deployable exists. | [V] `os.walk` listing; `PermissionError` on index-template.md; `ls .github` → absent | Delete/recreate `index-template.md` as a real file, complete the mirror skeleton (add module-08), author `index.md` + Pages workflow per the documented Stage-2 spec, and verify a local build. | OPEN |
| H3 | **HIGH** | Labs | **Lab infrastructure artifacts do not exist and instructions are largely untested.** No VM images, prepared captures, log datasets, or evidence packs in the repo; none of tshark/lynis/nmap/john/hashcat present in the authoring environment; only Lab 15's starter was executed (sha256/hmac paths). All other practical steps are unverified. | [V] repo inventory (no artifacts); `command -v` checks all ABSENT; starter `--help` + sha256 path ran | Build the infrastructure pack to the acceptance specs in each worksheet (snapshots, PCAPs, datasets, evidence packs), then perform a first-delivery walkthrough of every core lab and move items from "untested" to "tested" in the lab validation ledger. | OPEN |
| M1 | MEDIUM | Slides | **Slide rendering untested.** Marp CLI absent; the HTML/PDF/PPTX pipeline has never been executed; only Markdown-level structure is validated. | [V] `marp: ABSENT`; slides validator README check | Install `@marp-team/marp-cli`, render one deck to HTML + PDF, check theme output and pagination, then render all 35 decks; record results in the slides report. | OPEN |
| M2 | MEDIUM | Governance / Calendar | **Instructor planning calendar generated outside the instructor-only tree.** `calendar/generated/instructor-calendar.md` is marked ⚠️ in its title but the path is not instructor-only; `calendar/README.md` claims instructor-materials distribution rules apply without the path enforcing them. | [V] file exists in `calendar/generated/`; README wording inspected | Route the instructor view to `instructor-materials/instructor-only/calendar/` at generation time (or exclude it from any public publish list); align README wording with actual behavior. | OPEN |
| M3 | MEDIUM | Docs accuracy | **Root README is stale.** States "🚧 architecture phase" although the package is fully authored, and describes `case-studies/` as "real-world incident analyses" although every case is labeled simulated and the collection forbids presenting fiction as real incidents. | [V] README head inspected vs current tree | Rewrite the status block and the case-studies description (simulated scenarios); add pointers to calendar, validators, and the instructor-tier migration requirement. | OPEN |
| M4 | MEDIUM | Technical accuracy | lecture-16 note calls WPA3's known research issues "**Dragonslayer**-class" — the published research set is **Dragonblood** (Vanhoef & Piessens, 2019, ETH Zurich). Wrong name for a named research result. | [V] line 17 of `lectures/module-04-network/lecture-16.md` | Correct to "Dragonblood-class (Vanhoef & Piessens, 2019)" and add the paper to the lecture's reference list. | OPEN |
| M5 | MEDIUM | Usability / Links | Authored documents reference other files via backtick paths, not Markdown links — **0 internal md links repo-wide** (verified; generated calendars do have working relative links). Navigation between syllabus ↔ labs ↔ cases ↔ assessments requires manual path hunting. | [V] repo-wide grep `\]\(` = 0 (excluding generated) | Convert high-traffic references (README map, syllabus §8/§9, lab/case indexes, instructor manual) to relative Markdown links; the calendar's link validator pattern can be reused for CI. | OPEN |
| M6 | MEDIUM | Assessments | **Capstone report/defense mark split is ambiguous.** Instructor rubric scores the report /100 and scales the 20-pt defense rubric ×1.5, but the student brief states only combined weights (report sections 100 pts; milestone "10% inside report marks"). Students cannot compute their component split; the ×1.5 derivation is undocumented. | [V] `capstone-rubric.md` vs `capstone-brief.md` wording | State the final composition explicitly in both files (e.g., report 70 / defense 30 of the 20% component) and show the defense scaling arithmetic. | OPEN |
| L1 | LOW | Teaching notes | lecture-32 (showcase/exam) note lacks DS-track-specific content — the only one of 32 notes without it (grep-verified). The paired slide deck does include DS answer-shape guidance. | [V] `grep -LiE 'data science|ds-track'` → lecture-32 only | Add a short DS-track defense-prep paragraph to the L32 note for consistency. | OPEN |
| L2 | LOW | CLO mapping | Per-note CLO tags are thin for CLO-3 and CLO-5 (each appears in only 4 of 32 student notes; other CLOs in 5–7). The authoritative syllabus matrix is complete; notes tag only their primary/supporting CLOs. | [V] per-CLO note counts | Add explicit CLO tags in M3/M5 notes where supporting outcomes apply, or document the tag convention (primary-only) in `lectures/README.md`. | OPEN |
| L3 | LOW | Labs | Lab 15 starter's `aead`/`kdf` subcommands untested (cryptography package absent) — verified they degrade gracefully with course-VM pointers rather than crashing. | [V] ran both subcommands; output printed VM instructions | Test the full starter on the course VM image when it exists; then mark the paths tested in the lab validation ledger. | OPEN |
| L4 | LOW | Validation | The python-vs-openssl digest cross-check could not be completed this session (Windows/MSYS `/tmp` path split broke the fixture); the identical-digest claim in the lab validation report therefore rests on the earlier recorded run, not this audit. | [N] command failed on environment path handling; not a content defect | Re-run the cross-check with a workspace fixture path; note it in the lab ledger. | OPEN |
| I1 | INFO | All validators | All four repo validators pass: cases (100/100, all checks), assessments (all checks), slides (32+2+1 decks, all checks), calendar (16/32/no gaps/links). | [V] executed, exit 0 each | Keep validators in CI; extend the assessments validator with the checkpoint paper↔key item-coverage check that found H1. | OPEN |
| I2 | INFO | Accessibility | Theme contrast passes WCAG AA comfortably: body 18.9:1, headings 11.2:1, emphasis 10.3:1, footer 7.5:1 (computed WCAG ratios). | [V] computed | None. | CLOSED |
| I3 | INFO | Content hygiene | No TODO/TBD/PLACEHOLDER markers; no replacement characters or CJK/Arabic script anomalies in any md file. | [V] repo-wide scans | None. | CLOSED |
| I4 | INFO | Safety | Prohibited-pattern scan hits are all ATT&CK technique names (e.g., "OS Credential Dumping T1003") in defensive/detection context — compliant with the responsible-use rules. | [V] context read of all 4 hits | None. | CLOSED |
| I5 | INFO | Technical accuracy | `openssl enc -aes-256-gcm` failure ("AEAD ciphers not supported") reproduced — the taught limitation is factual. | [V] executed | None. | CLOSED |
| I6 | INFO | Website docs | The absence of a Pages workflow matches the documented Stage-2 deferral ("site never deploys empty") — docs and reality are consistent, but see H2 for the anomalous tree. | [V] website/README vs tree | None beyond H2. | CLOSED |
| I7 | INFO | Labs | All 8 core lab worksheets carry the 16 required components (verified after correcting auditor regex errors — first-pass 0/8 results on three components were checker false negatives, re-run with fixed alternation patterns). | [V] component grep, 8/8 on all components | None. | CLOSED |

**Tally:** CRITICAL 1 · HIGH 3 · MEDIUM 6 · LOW 4 · INFORMATIONAL 8 (3 closed, 5 open-keep).
