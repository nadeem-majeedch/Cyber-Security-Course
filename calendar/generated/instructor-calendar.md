# ⚠️ INSTRUCTOR-ONLY — moved

The instructor planning calendar is generated directly to `instructor-materials/instructor-only/calendar/instructor-calendar.md` so it never sits in a publishable directory. Regenerate with `python calendar/tools/generate_calendar.py --start YYYY-MM-DD`.
